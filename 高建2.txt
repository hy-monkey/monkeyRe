1、简单说一下你在项目中的开发流程
	需求分析-->项目设计，程序开发-->测试--->部署上线

2、使用limit进行分页,起始偏移量是limit (pageSize - 1) * 每页的数据量,每页的数据量

4、入参校验，业务控制流程，业务规则控制

5、cookie是存储于客户端的，它可以实现客户端与服务器之间状态的保持

6、禁用客户端cookie服务器session不能使用，因为session依赖于cookie，服务器会返回浏览器一个jsessionid，以保存到 
     客户端的cookie中

7、数据回填就是将你查出来的数据返回给前台的文本框，也就是数据显示在文本框内


8、<c:forEach var = "item" items="集合或者数组">
          <c:out value="${item}"
     </c>

9、select * from users limit x(起始位置),x(每页数据量)

10、1通过总数据大小/每页数据量得到一共多少页
        2通过(pageSize - 1)  * 每页数据量大小，可得到每页的起始数据位置
        3以当前页为准，一共只显示9个按钮，上一页，下一页，两个省略号，和具体分页数字
          当前页-2如果等于1，那么显示1到6的数字按钮，后面是省略号和下一页，如果+2等于总页数和之前相反

11、事务是一个控制单元，所有在这个控制单元的事务，或者说操作，要么一起完成，如果有一个失败，全部失败
       关闭自动提交--->commit--->失败rollback()     前提必须是一个连接

12、记住密码，权限校验，全站压缩